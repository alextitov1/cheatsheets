Some of the methods to expose internal network are service types:

* NodePort
* LoadBalancer

Some use their own API reosurces:
* Ingress
* Route

The following termination types are available:

* Edge
* Passthrough
* Reencrypt
